# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# Scripts that pull from the Census API load this via os.environ.

# U.S. Census Bureau API key (free; register at https://api.census.gov/data/key_signup.html)
CENSUS_API_KEY=
